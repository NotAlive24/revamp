Access is assigned based on job role, so users automatically get or lose permissions when they enter or leave that role.

In simple words, If a user enters, the user gets assigned a role automatically, and once the user leaves it gets unassigned and the role gets deleted.